# AGENTS.md

## 项目说明

OpenPortfolio 是一个本地优先、隐私友好的全球投资资产管理工具，用于记录股票、基金、现金和多币种资产。

首个版本为纯前端静态 Web 应用，并通过 GitHub Pages 发布。

## 开始开发前必须阅读

在进行架构、数据模型或核心业务修改前，必须阅读：

* `docs/PRD.md`
* `docs/ARCHITECTURE.md`
* `docs/DATA_MODEL.md`

以上文档是当前项目的主要设计依据。

如果实现需求与现有文档发生冲突，应先说明冲突，不得直接自行修改既定规则。

## 当前 MVP 目标

当前阶段需要让用户能够：

1. 创建投资账户。
2. 添加 A 股、美股、基金和现金持仓。
3. 手动输入资产价格和 USD/CNY 汇率。
4. 计算持仓成本、市值和盈亏。
5. 同时查看人民币和美元口径的总资产。
6. 将全部数据保存在本地。
7. 导出完整备份并能够恢复。

项目初始化阶段不要接入自动行情接口。

## 技术栈

使用：

* Vue 3
* TypeScript
* Vite
* Vue Router
* Pinia
* IndexedDB
* Dexie.js
* Vitest

为了兼容 GitHub Pages，路由使用 Hash 模式。

## 架构规则

* Vue 页面和组件不得直接调用 Dexie。
* 持久化数据必须通过 Repository 和 Service 层访问。
* 页面不得直接请求外部行情接口。
* 金融计算逻辑必须放在独立的纯函数中。
* 除历史快照外，不重复存储可以计算得到的数据。
* 应优先加载本地数据，再考虑远程更新。
* 必须永久保留手动录入价格的能力。
* 不得添加服务器、云数据库、登录系统或遥测功能。
* 不得上传用户持仓和资产数据。

## 开发规则

* 每次只完成一个较小、可检查的任务。
* 不要在一次任务中实现整个 PRD。
* 修改完成后运行类型检查和测试。
* 完成任务后说明修改和新增了哪些文件。
* 说明实际运行过的命令。
* 说明测试和构建结果。
* 明确列出仍未解决的问题。
* 避免与当前任务无关的重构。
* 不得擅自替换已经确定的架构方案。

## 数据安全规则

* 正常初始化过程中不得清空或覆盖 IndexedDB。
* 数据库结构变化必须使用明确的 Dexie 版本和迁移逻辑。
* 导入备份前必须验证文件格式和数据内容。
* 金额计算不得过早四舍五入。
* 缺失价格必须明确标记，不得自动使用成本价代替当前价格。

## 文档维护

如果任务修改了产品规则、技术架构或数据库结构，应同步更新对应文档：

* 产品规则：`docs/PRD.md`
* 技术架构：`docs/ARCHITECTURE.md`
* 数据模型：`docs/DATA_MODEL.md`

除非实现确实需要，否则不要创建大量不必要的文档。
